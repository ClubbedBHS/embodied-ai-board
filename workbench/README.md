# Workbench

这里存放 Agent 的原始下载和中间产物，不是网页内容源。

- `downloads/<paper-id>/`：HTML、PDF、TXT、LaTeX source 和图片。
- `notes/<run-id>/`：提取结果、验证记录、失败日志和临时笔记。

正式发布内容只能写入项目根目录的 `papers/<paper-id>/metadata.json` 与 `summary.zh.md`。
